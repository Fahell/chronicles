================================================================================
TEST PROMPT — PERCHANCE RUNTIME VALIDATION ROUND 1
Project: RPG Perchance generator (type-C open scene slice, MVP)
Date: 2026-08-15
================================================================================

You are the runtime validation agent for this RPG generator. The AI text and
image plugins only execute inside the Perchance platform, and the generator
runs in a cross-origin iframe — so you are the only one who can validate the
real plugin behavior. Local tests + CI are already green; your job is to
validate what only runs here: the real `generateImage` / `generateText` calls.

Before you start, read `src/README.md` for the project context and the
platform↔code mapping.

--------------------------------------------------------------------------------
1. BOOT CHECK
--------------------------------------------------------------------------------
- Load the generator. It should boot with no console errors.
- The scene is a 3D open landscape (three.js): a floor plane and a landscape
  backdrop, plus two placeholder character sprites (labeled "player" and
  "elder") standing on the floor.
- Report: does the page load? Do you see the scene? Any console errors
  (paste them verbatim)? Note anything that is missing or broken.

--------------------------------------------------------------------------------
2. IMAGE GENERATION (the floor + backdrop)
--------------------------------------------------------------------------------
The two scene textures (floor + backdrop) are generated by the image plugin
on first load. Expected: the floor is a grassy meadow texture, the backdrop
is a twilight landscape with a distant castle on the right and rocky spires
on the left (pixel-art style, per the prompts in `src/rpg/src/scene/
manifest/openPlains.ts`).

- Confirm both textures generate and appear (floor under the characters,
  backdrop behind them).
- Note: generation may take a while — do NOT treat a slow generation as a
  failure. Wait for it.
- If a texture looks wrong (artifacts, wrong content, unusable), report it —
  the asset can be regenerated by the player later (regeneration UX is a
  future milestone; do not look for it yet).
- Report: how long did each generation take (approx.)? Did both textures
  apply correctly? Any errors during generation?

--------------------------------------------------------------------------------
3. DIALOGUE FLOW
--------------------------------------------------------------------------------
Click the "Talk to the elder" button (top-left HUD).

Expected:
- A dialogue box appears at the bottom with speaker "Elder" and some text.
- If the AI chose to include options, they appear as numbered buttons inside
  the box (format: `[choices]` block in the AI response, parsed by the app).
- Clicking an option ends the turn.
- The "Leave" button is ALWAYS present and ALWAYS ends the interaction
  (always-escape — the player can never get stuck in a dialogue).
- Click "Talk to the elder" again and click "Leave" — the box must close.

Report: does the dialogue open? Does the text render? Do options appear and
work when the AI offers them? Does "Leave" always close the box?

--------------------------------------------------------------------------------
4. RELOAD / CACHE BEHAVIOR
--------------------------------------------------------------------------------
Reload the generator. Expected: the floor and backdrop textures are served
from the local cache (IndexedDB) — they should NOT be regenerated, and the
page should load noticeably faster than the first time.

Report: after reload, do the same textures appear (no visual change)? How
long did the second load take vs. the first?

--------------------------------------------------------------------------------
5. GENERAL OBSERVATIONS
--------------------------------------------------------------------------------
- Any visual glitches, clipping, wrong scaling, or layout problems?
- Is the HUD readable and the dialogue box usable?
- Anything that crashed or froze the generator?

--------------------------------------------------------------------------------
REPORT FORMAT
--------------------------------------------------------------------------------
Reply with a structured report:

1. BOOT: pass / fail + details + console errors (verbatim)
2. IMAGES: pass / fail + generation times + texture quality notes
3. DIALOGUE: pass / fail + options behavior + always-escape confirmation
4. RELOAD/CACHE: pass / fail + timing comparison
5. OBSERVATIONS: anything notable

End with a summary line: how many checks passed, and the single most
important issue to fix (if any). Do NOT modify the code — report only.
================================================================================
