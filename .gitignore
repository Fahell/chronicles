# Infraestrutura do ambiente (Freebuff) — não faz parte do projeto
# (contém perfil de navegador, chaves de API e config MCP)
.agents/

# Perchance — artefatos efêmeros / auto-baixados
scratch/
imports/

# Node
node_modules/

# Build
dist/
*.tsbuildinfo

# The ONNX Runtime wasm asset Vite emits from transformers.js (lazy chunk).
# Never fetched at runtime (bg-removal uses the jsdelivr CDN wasmPaths,
# verified available) and 23.5 MB — keep it out of git and the ship.
rpg/build/assets/

# Graphify — generated artifacts (knowledge graph)
graphify-out/
raw/

# Bundle analysis reports (regenerable with pnpm analyze)
rpg/reports/

# Segredos
.env
.env.*
!.env.example

# SO / editores
.DS_Store
Thumbs.db
.idea/
.vscode/
